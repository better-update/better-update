{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["Cargo.toml", "build.rs", "src/**", "package.json"],
      "outputs": ["index.js", "index.d.ts", "*.node"]
    }
  }
}
