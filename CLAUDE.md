# Project Agent Guidance

## Script Execution and Dependency Management

- Package manager: `bun`, for script execution (`bunx`) and dependency management. Do not use `npm`, `npx`, or `yarn` for running scripts or managing dependencies.
- Preferred package.json scripts over direct command execution for better maintainability and consistency.
